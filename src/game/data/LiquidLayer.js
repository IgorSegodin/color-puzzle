// TODO remove if not needed
export default class LiquidLayer {

    /**
     * @param color {String}
     * @param fillFraction {Number}
     */
    constructor({color, fillFraction}) {
        this.color = color;
        this.fillFraction = fillFraction;
    }
}